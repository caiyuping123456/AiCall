现在有以下几个问题，请你帮我解决一下：
用户端：
1. 用户端发起挂号这里应该要保持幂等性，现在的问题就是用户端进入挂号的流程后，会发送很多条通知，导致医生端收到很多条通知，这个问题应该怎么解决呢？
2. 用户端发起挂号后，用户再次挂号同一个医生会显示已经挂号（一天只能挂同一个医生一次）
3. 用户端进入挂号后，如果整个流程没有完成，用户再次进入后需要重新开始（同时后端不会存储中间一半的信息）

医生端：
1. 医生端查看会诊列表时，生成AI摘要会发生异常（这里需要解决），但是也会生成摘要信息
2. 同时对于已经完成的会诊，医生端进入查看时还是显示会诊进行中，这个问题需要修复一下。
3. 对于生成的报告，显示的是json格式的字符串，医生端需要解析这个字符串并以更友好的方式展示出来。
4. 同时呢，对于用户端来说，用户端应该也需要看到这个流程，比如医生签发了会诊报告，用户端也应该能够看到这个报告的内容，这个问题也需要解决一下。

管理端：
1. 管理端应该也要追踪这些（用户和医生的会诊流程），管理管也能看到这些流程的状态和相关信息，这样管理端也能更好地进行管理和监督。
2. 同时前端还有一些问题，数据可视化这里还是有一点问题。